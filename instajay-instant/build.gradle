apply plugin: 'com.android.instantapp'

android {
    compileSdkVersion project.compileSdkVersion
    buildToolsVersion project.buildToolsVersion
}

dependencies {
    implementation project(":instajay-base")
}
